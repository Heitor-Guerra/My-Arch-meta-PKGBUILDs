pkgname=my-arch-metapkg
pkgver=1.0
pkgrel=1
pkgdesc="Meta-package for my Arch Linux setup. AMD CPU, lvm"
arch=('any')
depends=(
    # Basic Install Packages
    linux-lts linux-firmware   amd-ucode base base-devel efibootmgr   wl-clipboard openssh git htop ufw foot
    # Fonts
    ttf-liberation ttf-jetbrains-mono-nerd

    #Programming Languages
    jdk-openjdk 
    # Programming Tools
    cmake docker docker-compose uv
    
    # CLI Programs for Basic Usage (Video, Editing, File management)
    7zip mpv neovim yazi 
    # Other GUI Programs
    firefox phototonic steam
    

    # Desktop Environment Packages (KDE)
    bluedevil kscreen plasma-desktop plasma-nm plasma-pa sddm-kcm spectacle
)
url="https://github.com/Heitor-Guerra"
license=('GPL')

package() {
    echo "Meta-package installed."
}
